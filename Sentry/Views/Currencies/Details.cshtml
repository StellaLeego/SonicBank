@using Open.Facade.Money
@using Open.Sentry.Extensions
@model Open.Facade.Money.CurrencyViewModel

@{
    ViewBag.Title = "Details";
    ViewBag.SubTitle = "Currency";
    Layout = "_DetailsPartial";
}

<body>
    @Html.ViewingControlsFor(model => model.IsoCurrencySymbol)
    @Html.ViewingControlsFor(model => model.CurrencySymbol)
    @Html.ViewingControlsFor(model => model.Name)
    @Html.ViewingControlsFor(model => model.ValidFrom)
    @Html.ViewingControlsFor(model => model.ValidTo)
</body>

@section Actions
    {
    @Html.ActionLink("Edit", "Edit", new { id = Model.IsoCurrencySymbol})
    @Html.ActionLink("Back to List", "Index")
}
